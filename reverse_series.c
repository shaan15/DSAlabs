#include<stdio.h>
#define N 10
in main(void)
{
int a[n],i;
printf("enter %d numbers; ", N)
for(i=0;i<N;i++)
{ scanf("%d",&a[i]); }
printf("in rev order");
for(i=N-1;i>=0;i--)
{ printf("%d",a[i]; )
printf("\n");
return 0;
}
